import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prototipo-mapa-PET',
  webDir: 'www/browser',
};

export default config;
